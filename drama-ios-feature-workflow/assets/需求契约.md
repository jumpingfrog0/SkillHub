# <需求名称> 需求契约

## 契约状态

当前状态：<草稿 / 待确认 / 已确认>

说明：

- 有未决事项时，当前状态只能是 `草稿` 或 `待确认`。
- 用户明确回复 `CONFIRM_REQUIREMENT` 后，才允许标记为 `已确认`。
- 只有当前状态为 `已确认`，才允许进入技术评审。
- 本文件只确认用户可见结果和外部能力语义；除多语言 key 外，不记录客户端内部实现或精确协议/字段标识。

## 功能范围

1. <明确本需求要实现的用户可见能力和目标页面>
2. <明确本需求要实现的用户可见能力和目标页面>

## 非目标

1. 不重构无关模块。
2. 不修改未列入计划的公共行为。
3. 不猜测外部能力、消息业务语义、设计材料和 i18n key。

## 业务规则

### <规则分组>

1. <业务规则 1>
2. <业务规则 2>

## 外部能力与业务约束

| 所需能力 | 业务语义与用户可见约束 | 来源 | 确认状态 |
|---|---|---|---|
| 服务端能力 | <不涉及 / 已确认：客户端需要获得或提交的业务信息及其语义> | <待填写> | <待确认 / 已确认 / 不涉及> |
| 消息能力 | <不涉及 / 已确认：消息业务类型、展示所需信息和处理规则> | <待填写> | <待确认 / 已确认 / 不涉及> |
| 配置能力 | <不涉及 / 已确认：配置控制的用户可见行为> | <待填写> | <待确认 / 已确认 / 不涉及> |

不得填写接口名、字段名、消息 subtype、客户端类/方法或实现链路；这些内容在 `技术方案.md` 的需求语义映射中确认。

## 设计、跳转与内容约束

| 项目 | 用户可见约束 | 来源 | 确认状态 |
|---|---|---|---|
| 设计材料 | <不涉及 / 已确认：设计稿、交互稿或视觉要求> | <待填写> | <待确认 / 已确认 / 不涉及> |
| 路由跳转 | <不涉及 / 已确认：用户到达的目标和业务参数语义> | <待填写> | <待确认 / 已确认 / 不涉及> |
| 内容展示 | <不涉及 / 已确认：图片、文案或奖励等用户可见要求> | <待填写> | <待确认 / 已确认 / 不涉及> |

不得填写现有资源名、资源路径或路由实现；复用与接入方式在技术方案中确定。

## 多语言文案

新增文案只确认 key，不手动修改 `lang.json`、`Localizable.strings` 等多语言产物。多语言文件由开发者通过自动化脚本更新。

多语言 key 是本需求契约唯一允许保留的精确实现标识。

| 场景 | 建议 key | 默认中文 | 默认英文 | 状态 |
|---|---|---|---|---|
| <待填写> | <待填写> | <待填写> | <待填写> | <待确认 / 已确认 / 不涉及> |

## 验收标准

| 场景 | 操作 | 预期结果 |
|---|---|---|
| <待填写> | <待填写> | <待填写> |

## 未决事项

- [ ] <待确认事项和解除条件>
