# <需求名称> 技术方案

## 审批后维护规则

本文件在审批前用于技术评审和方案收敛。收到 `APPROVE_PLAN` 后，本文件成为该次审批对应的技术方案基准。

审批后不得按最终代码反向改写方案正文。本文件不承载最终实现复盘，实际交付、方案偏离、验证结果和风险写入 `handoff.md`。

如编码中发现必须扩大影响范围或改变已审批方案，先暂停编码并记录到 `decision_log.md`；必要时更新本文件和 `approval.md` 形成新的审批基准，用户确认后再继续。

## 目标

<用工程语言描述本次要完成的能力。>

## 非目标

1. 不扩大 `requirement_contract.md` 中未列出的业务范围。
2. 不新增未审批依赖。
3. 不修改代码混淆相关文件和 IR mapping JSON。

## 选定方案

<填写唯一选定方案。进入审批前不得保留未收敛结论。>

## 影响范围

| 模块或文件 | 操作 | 说明 |
|---|---|---|
| <待填写> | <新增 / 修改 / 不修改> | <待填写> |

## iOS 工程接入

若本方案新增 Objective-C/Swift 源码文件，必须把 `Drama.xcodeproj/project.pbxproj` 列入影响范围，用途仅限工程引用和 target membership。

| 检查项 | 结论 |
|---|---|
| 是否新增 `.h/.m/.mm/.swift` 文件 | <是 / 否> |
| 是否需要更新 `Drama.xcodeproj/project.pbxproj` | <是 / 否> |
| 是否新增资源或 assets | <是 / 否> |
| 是否需要加入 target resources | <是 / 否> |

## 关键数据流和状态流

| 链路 | 数据来源 | 处理方式 | 输出 |
|---|---|---|---|
| <待填写> | <待填写> | <待填写> | <待填写> |

## 风险

| 风险 | 影响 | 缓解方案 | 状态 |
|---|---|---|---|
| <待填写> | <待填写> | <待填写> | <开放 / 已缓解 / 已接受> |

## 验证计划

| 验证项 | 方法 | 预期结果 |
|---|---|---|
| <待填写> | <静态检查 / 人工验证 / 单元测试 / CI> | <待填写> |
