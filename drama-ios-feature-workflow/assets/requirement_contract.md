# <需求名称> 需求契约

## 契约状态

当前状态：<草稿 / 待确认 / 已确认>

说明：

- 有未决事项时，当前状态只能是 `草稿` 或 `待确认`。
- 用户明确回复 `CONFIRM_REQUIREMENT` 后，才允许标记为 `已确认`。
- 只有当前状态为 `已确认`，才允许进入技术评审。

## 功能范围

1. <明确本需求要实现的用户可见能力和目标页面>
2. <明确本需求要实现的用户可见能力和目标页面>

## 非目标

1. 不重构无关模块。
2. 不修改未列入计划的公共行为。
3. 不猜测服务端接口、消息协议、图片资源和 i18n key。

## 业务规则

### <规则分组>

1. <业务规则 1>
2. <业务规则 2>

## 接口和协议

| 项目 | 结论 | 来源 |
|---|---|---|
| 服务端接口 | <不涉及 / 已确认：接口名和字段> | <待填写> |
| 消息协议 | <不涉及 / 已确认：协议名和字段> | <待填写> |

## 资源和文案

| 项目 | 结论 | 来源 |
|---|---|---|
| 图片资源 | <不涉及 / 已确认：资源路径和命名> | <待填写> |
| 路由跳转 | <不涉及 / 已确认：目标和参数> | <待填写> |

## 多语言文案

新增文案只确认 key，不手动修改 `lang.json`、`Localizable.strings` 等多语言产物。多语言文件由开发者通过自动化脚本更新。

| 场景 | 建议 key | 默认中文 | 默认英文 | 状态 |
|---|---|---|---|---|
| <待填写> | <待填写> | <待填写> | <待填写> | <待确认 / 已确认 / 不涉及> |

## 验收标准

| 场景 | 操作 | 预期结果 |
|---|---|---|
| <待填写> | <待填写> | <待填写> |

## 未决事项

- [ ] <待确认事项和解除条件>
