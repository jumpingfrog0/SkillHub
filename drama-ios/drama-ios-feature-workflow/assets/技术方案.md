# <需求名称> 技术方案

## 审批后维护规则

本文件在审批前用于技术评审和方案收敛。收到 `APPROVE_PLAN` 后，本文件成为该次审批对应的技术方案基准。

审批后不得按最终代码反向改写方案正文。本文件不承载最终实现复盘，实际交付、方案偏离、验证结果和风险写入 `交付文档.md`。

如编码中发现必须扩大影响范围或改变已审批方案，先暂停编码并记录到 `决策日志.md`；必要时更新本文件和 `计划审批.md` 形成新的审批基准，用户确认后再继续。

## 目标

<用工程语言描述本次要完成的能力。>

## 非目标

1. 不扩大 `需求契约.md` 中未列出的业务范围。
2. 不新增未审批依赖。
3. 不修改代码混淆相关文件和 IR mapping JSON。

## 选定方案

<填写唯一选定方案。进入审批前不得保留未收敛结论。>

## 需求语义 → 实现映射

本节将已确认的需求契约映射为唯一实现方案。精确接口、协议/字段、资源、内部模块、文件、数据流和失败策略只在本节及后续技术章节记录。

| 需求契约语义 | 精确外部接口、协议或资源 | 内部实现范围 | 状态/失败处理 | 验证方式 |
|---|---|---|---|---|
| <待填写> | <待填写> | <待填写> | <待填写> | <待填写> |

## 影响范围

| 模块或文件 | 操作 | 说明 |
|---|---|---|
| <待填写> | <新增 / 修改 / 不修改> | <待填写> |

## iOS 工程接入

若本方案新增 Objective-C/Swift 源码文件，必须把 `Drama.xcodeproj/project.pbxproj` 列入影响范围，用途仅限工程引用和 target membership。

| 检查项 | 结论 |
|---|---|
| 是否新增 `.h/.m/.mm/.swift` 文件 | <是 / 否> |
| 是否需要更新 `Drama.xcodeproj/project.pbxproj` | <是 / 否> |
| 是否新增资源或 assets | <是 / 否> |
| 是否需要加入 target resources | <是 / 否> |

## 设计稿精读与 UI 落地门禁

需求涉及设计稿时使用 `$implement-ui-from-design-images`。不涉及时将本节结论填写为“不涉及”。

| 项目 | 结论 |
|---|---|
| 调用工作流 | `drama-ios-feature-workflow` |
| 调用阶段 | <技术评审 / 已审批 / 不涉及> |
| 执行模式 | <implementation-read / 不涉及> |
| 精读授权 | <true / false / 不涉及> |
| 任务根目录或设计目录 | <待填写> |
| 设计输入 fingerprint | <首次精读后填写 / 不涉及> |
| 设计精读时机 | <计划审批前 / 用户明确批准延迟到 APPROVE_PLAN 后、UI 编码前 / 不涉及> |
| 设计材料状态 | <完整 / 阻塞 / 不涉及> |
| 设计精读门禁 | <通过 / 待执行 / 阻塞 / 不涉及> |
| UI 职责划分 | <待填写> |
| 组件处理策略 | <直接复用 / 最小修改 / 专用组件 / 重新划分 / 不涉及；填写稳定结论> |
| 资源处理策略 | <复用 / 迁移 / 新增 / 不涉及；填写平台接入要求> |
| 预授权调整范围 | <待填写 / 不涉及> |
| 超出范围时的处理 | <退回需求确认 / 退回计划审批 / 不涉及> |

详细像素、间距、坐标和颜色采样不写入本文件。

## 关键数据流和状态流

| 链路 | 数据来源 | 处理方式 | 输出 |
|---|---|---|---|
| <待填写> | <待填写> | <待填写> | <待填写> |

## 风险

| 风险 | 影响 | 缓解方案 | 状态 |
|---|---|---|---|
| <待填写> | <待填写> | <待填写> | <开放 / 已缓解 / 已接受> |

## 验证计划

| 验证项 | 方法 | 预期结果 |
|---|---|---|
| <待填写> | <静态检查 / 人工验证 / 单元测试 / CI> | <待填写> |

涉及设计稿时补充视觉层级、关键状态、不同屏幕尺寸、长文案、RTL、资源显示和共享组件影响验证；无法执行的项目明确记录原因。
